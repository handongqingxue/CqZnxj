<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cqZnxj.dao.PatLineAreaAccSetMapper">
	<select id="queryForInt" resultType="java.lang.Integer">
		select count(*) from pat_line_area_acc_set plaas,patrol_line pl,patrol_area pa where plaas.plId=pl.id and plaas.paId=pa.id
		<if test="plName!=''&amp;plName!=null">
			and pl.name like CONCAT('%',#{plName},'%')
		</if>
		<if test="paName!=''&amp;paName!=null">
			and pa.name like CONCAT('%',#{paName},'%')
		</if> 
	</select>
	<select id="queryList" resultType="com.cqZnxj.entity.PatLineAreaAccSet">
		select plaas.*,pl.name plName,pa.name paName from pat_line_area_acc_set plaas,patrol_line pl,patrol_area pa where plaas.plId=pl.id and plaas.paId=pa.id
		<if test="plName!=''&amp;plName!=null">
			and pl.name like CONCAT('%',#{plName},'%')
		</if>
		<if test="paName!=''&amp;paName!=null">
			and pa.name like CONCAT('%',#{paName},'%')
		</if> 
		limit #{rowNum},#{rows}
	</select>
</mapper>