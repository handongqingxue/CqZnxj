<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cqZnxj.dao.PatrolAreaMapper">
	<insert id="add" parameterType="com.cqZnxj.entity.PatrolArea">
		insert into patrol_area
		<trim prefix="(" suffix=")" suffixOverrides=",">
			<if test="name!=null">
				name,
			</if>
				createTime,
			<if test="pdaIds!=null">
				pdaIds,
			</if>
			<if test="deptId!=null">
				deptId,
			</if>
		</trim>
		values
		<trim prefix="(" suffix=")" suffixOverrides=",">
			<if test="name!=null">
				#{name},
			</if>
				now(),
			<if test="pdaIds!=null">
				#{pdaIds},
			</if>
			<if test="deptId!=null">
				#{deptId},
			</if>
		</trim>
	</insert>
	<select id="queryForInt" resultType="java.lang.Integer">
		select count(*) from dept d,patrol_area pa where d.deptId=pa.deptId
		<if test="name!=''&amp;name!=null">
			and pa.name like CONCAT('%',#{name},'%')
		</if>
		<if test="deptId!=''&amp;deptId!=null">
			and d.deptId=#{deptId}
		</if>
		<if test="deptName!=''&amp;deptName!=null">
			and d.deptName like CONCAT('%',#{deptName},'%')
		</if> 
	</select>
	<select id="queryList" resultType="com.cqZnxj.entity.PatrolArea">
		select pa.*,d.deptName from dept d,patrol_area pa where d.deptId=pa.deptId
		<if test="name!=''&amp;name!=null">
			and pa.name like CONCAT('%',#{name},'%')
		</if>
		<if test="deptId!=''&amp;deptId!=null">
			and d.deptId=#{deptId}
		</if>
		<if test="deptName!=''&amp;deptName!=null">
			and d.deptName like CONCAT('%',#{deptName},'%')
		</if> 
		limit #{rowNum},#{rows}
	</select>
</mapper>